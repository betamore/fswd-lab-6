extends ../layout.pug
block body
  h1 Tasks!

  ul
    each task in tasks
      li
        a(href="/tasks/" + task.id)= task.name
        if highlightTaskId == task.id
          p THIS IS THE NEW ONE!
